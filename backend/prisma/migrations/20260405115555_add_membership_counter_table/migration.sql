-- CreateTable
CREATE TABLE `membershipCounter` (
    `year` INTEGER NOT NULL,
    `lastNumber` INTEGER NOT NULL,

    PRIMARY KEY (`year`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
